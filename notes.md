# Misc notes

Things I haven't scripted yet:

## Installing neovim

I have it installed in `opt` to get a more-recent release than Debian carries.

Also install the `python3-neovim` package, otherwise (it seems) Python evaluation won't work well enough for the Coqtail extension to work.

